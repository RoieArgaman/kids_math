/** Web Speech rate for student-facing Hebrew TTS (browser default is ~1). */
export const CHILD_TTS_RATE = 0.78;

/** Pause between primer speak chunks (ms). Longer pause aids Grade 1 comprehension. */
export const CHILD_TTS_CHUNK_GAP_MS = 700;
