# CI skip-path proof (throwaway)

This PR is a docs-only change used to demonstrate that the new CI
`changes` filter skips `lint-and-unit` and `e2e` while `security-scan`
still runs. It targets the PR #85 branch as its base so the diff is
100% non-code. Safe to close without merging.
